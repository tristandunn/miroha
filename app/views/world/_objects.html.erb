<%# locals: (current_room:) %>
<div>
  <label class="block text-xs font-medium text-slate-400 mb-1.5 uppercase tracking-wide">Objects (JSON)</label>
  <textarea
    data-world-editor-target="objects"
    data-action="blur->world-editor#saveObjects"
    class="w-full px-3 py-2 bg-slate-800 border border-slate-700 rounded text-xs text-slate-100 placeholder-slate-500 focus:outline-none focus:border-slate-600 focus:ring-1 focus:ring-slate-600 font-mono min-h-16 resize-none"
    placeholder='{"key": "value"}'><%= JSON.pretty_generate(current_room.objects) if current_room.persisted? && current_room.objects.present? %></textarea>
</div>
