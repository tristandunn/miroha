<%# locals: (current_room:) %>
<div class="group">
  <label class="block text-sm font-semibold text-slate-300 mb-2 flex items-center gap-2">
    <svg class="w-4 h-4 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
    </svg>
    Objects (JSON)
  </label>
  <div class="relative">
    <textarea
      data-world-editor-target="objects"
      data-action="blur->world-editor#saveObjects"
      class="w-full px-4 py-3 bg-slate-800/50 backdrop-blur-sm border border-slate-700/50 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500/50 focus:border-cyan-500/50 font-mono text-sm min-h-20 text-slate-100 placeholder-slate-500 transition-all duration-200 hover:bg-slate-800/70 resize-none"
      placeholder='{"painting": "A beautiful painting", "desk": "An old wooden desk"}'><%= JSON.pretty_generate(current_room.objects) if current_room.persisted? && current_room.objects.present? %></textarea>
    <div class="absolute top-2 right-2 px-2 py-1 bg-slate-900/80 backdrop-blur-sm rounded text-xs text-cyan-400 border border-cyan-500/30">
      JSON
    </div>
  </div>
</div>
