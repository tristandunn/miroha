<%# locals: (current_room:) %>
<div class="w-80 flex-shrink-0 p-3 h-full overflow-y-auto">
  <label class="block text-[10px] font-medium text-slate-500 mb-2 uppercase tracking-wider">Objects (JSON)</label>
  <textarea
    data-world-editor-target="objects"
    data-action="blur->world-editor#saveObjects"
    class="w-full h-32 px-2 py-1.5 bg-slate-950 border border-slate-800 rounded text-[10px] text-slate-100 placeholder-slate-600 focus:outline-none focus:border-slate-700 font-mono resize-none leading-tight"
    placeholder='{"key": "val"}'><%= JSON.pretty_generate(current_room.objects) if current_room.persisted? && current_room.objects.present? %></textarea>
</div>
