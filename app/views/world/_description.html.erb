<%# locals: (current_room:) %>
<div class="w-80 flex-shrink-0 border-r border-slate-800 p-3 h-full overflow-y-auto">
  <label class="block text-[10px] font-medium text-slate-500 mb-2 uppercase tracking-wider">Description</label>
  <textarea
    data-world-editor-target="description"
    data-action="blur->world-editor#saveDescription"
    class="w-full h-32 px-2 py-1.5 bg-slate-950 border border-slate-800 rounded text-xs text-slate-100 placeholder-slate-600 focus:outline-none focus:border-slate-700 resize-none"
    placeholder="Room description..."><%= current_room.description if current_room.persisted? %></textarea>
</div>
