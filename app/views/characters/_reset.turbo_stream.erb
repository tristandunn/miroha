<%# locals: (attacker_name:, character:, damage:, room:) %>
<%= turbo_stream.append("messages", partial: "monsters/attack/target/kill", locals: { attacker_name: attacker_name, damage: damage }) %>
<%= turbo_stream.replace("character", partial: "game/sidebar/character", locals: { character: character }) %>

<%= render "game/surroundings", room: room %>
<%= render "commands/look/success", description: room.description %>
