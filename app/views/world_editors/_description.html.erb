<%# locals: (current_room:) %>
<div class="border-t border-gray-700 bg-gray-800 p-6">
  <textarea
    data-world-editor-target="description"
    data-action="blur->world-editor#saveDescription"
    class="w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded focus:outline-none focus:border-blue-500 min-h-24 text-white resize-y"
    placeholder="Enter room description..."
  ><%= current_room.description if current_room.persisted? %></textarea>
</div>
